// See https://svelte.dev/docs/kit/types#app
//
// No top-level `import`/`export` here, so this file is an ambient declaration and does not need
// the `declare global` + `export {}` wrapper that the `create-svelte` template uses.
declare namespace App {
  // interface Error {}
  // interface Locals {}
  // interface PageData {}
  // interface PageState {}
  // interface Platform {}
}
