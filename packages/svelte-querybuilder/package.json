{
  "name": "svelte-querybuilder",
  "version": "0.1.1",
  "type": "module",
  "license": "MIT",
  "description": "Svelte component for complex query building",
  "keywords": [
    "svelte",
    "query",
    "builder",
    "query-builder",
    "filter",
    "sql"
  ],
  "author": "Jake Boone",
  "homepage": "https://github.com/react-querybuilder/svelte-querybuilder",
  "bugs": {
    "url": "https://github.com/react-querybuilder/svelte-querybuilder/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/react-querybuilder/svelte-querybuilder.git",
    "directory": "packages/svelte-querybuilder"
  },
  "svelte": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "sideEffects": [
    "*.css",
    "*.scss"
  ],
  "files": [
    "README.md",
    "dist",
    "!dist/**/*.test.*",
    "!dist/**/*.test-d.*",
    "!dist/**/*.spec.*"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "svelte": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./dist/*.css": "./dist/*.css",
    "./dist/*.scss": "./dist/*.scss",
    "./package.json": "./package.json"
  },
  "scripts": {
    "prepack": "bun run ./scripts/copy-readme.ts",
    "postpack": "rm -f README.md",
    "build": "bun run build:main && bun run build:css",
    "build:main": "svelte-package -i src/lib -o dist && find dist \\( -name '*.test.*' -o -name '*.test-d.*' -o -name '*.spec.*' \\) -delete",
    "build:css": "mkdir -p dist/styles && cp ../../node_modules/@react-querybuilder/core/dist/*.scss dist && cp ../../node_modules/@react-querybuilder/core/dist/styles/*.scss dist/styles && cp -f src/lib/styles/*.scss dist/styles 2>/dev/null || true; bun sass --style=compressed dist",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:exports": "bun run ./scripts/check-dist-specifiers.ts && attw --pack . --profile esm-only --ignore-rules internal-resolution-error",
    "conformance": "bun run conformance:fetch && bun run conformance:test",
    "conformance:fetch": "bun ./scripts/fetch-fixtures.ts",
    "conformance:test": "vitest run --config vitest.conformance.config.ts",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch"
  },
  "peerDependencies": {
    "svelte": "^5.25"
  },
  "dependencies": {
    "@react-querybuilder/core": "^8.22.3"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@sveltejs/package": "^2.5.8",
    "svelte": "^5.56.8",
    "svelte-check": "^4.7.4",
    "typescript": "^5.9.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
